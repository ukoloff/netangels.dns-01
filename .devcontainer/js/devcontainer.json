// For format details, see https://aka.ms/devcontainer.json. For config options, see the
{
  "name": "na01.js",
  "dockerComposeFile": "compose.yml",
  "service": "dev",
  "workspaceFolder": "/repo",
  "onCreateCommand": "apk add git lego",
  // "shutdownAction": "none",
  "postCreateCommand": "git clone -b js https://github.com/ukoloff/netangels.dns-01 .",
  "customizations": {
    "vscode": {
      "extensions": [
        "mhutchie.git-graph",
        "EditorConfig.EditorConfig"
      ]
    }
  }
}
